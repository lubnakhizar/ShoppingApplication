

@using ShoppingApplication.Models
@model List<User>
@{
    ViewBag.Title = "Index";
}

<h2>Users</h2>
<br />
<div>

    <div class="container">
        <div class="panel panel-primary">
            <div class="panel-heading">Users (  <a href="/User/Add" style="color:#2f107c"> <b>Add</b> </a>)</div>
            <div class="panel-body">
                <table class="table table-hover table-bordered">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Password</th>
                            <th>RoleId</th>                         
                            <th>Actions</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id </td>
                                <td>@item.Name</td>
                                <td>@item.Email</td>
                                <td>@item.Password</td>
                                <td>@item.RoleId</td>
                          
                                <td>
                                    <a href="/User/View/@item.Id"><span class="glyphicon glyphicon-eye-open"></span></a> |
                                    <a href="/User/Edit/@item.Id"><span class="glyphicon glyphicon-edit"></span></a> |
                                    <a href="/User/Delete/@item.Id"><span class="glyphicon glyphicon-trash"></span></a> |
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

        </div>

    </div>
</div>



